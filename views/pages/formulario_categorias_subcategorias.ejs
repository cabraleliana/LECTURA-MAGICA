<form id="categorias_subcategorias">

    <div class="form-group px-0 d-flex justify-content-center align-items-center" style="margin-top: 150px;">
            <select name="categoria" id="categoria"  class="ancho2 campo" onchange="on_change_categoria()">
                    <option value="">
                        Seleccione categoria
                    </option>
            </select>
    </div>
    <div class="form-group px-0 d-flex justify-content-center  align-items-center">
        <select name="subcategoria" id="subcategoria"  class="ancho1 campo" multiple='multiple'>
            </select>
    </div>
    
    <div class="d-flex">
        <button type="button" class="btn btn-secondary btn-block" onclick="limpiar_campos('.campo'); nueva_fila('#id_categoria')">Nuevo</button>
        <!-- limpiar campos limpia los valores de los input de la clase campo  y a su vez nuevo usuario le setea un valor 0 a un input oculto para que cuando demos guardar se sepa que es un nuevo usuario y que no es una modificacion -->
        <button type="button" class="btn btn-secondary btn-block" onclick="guardar_categoria_subcategoria()" >Guardar</button>
        <!-- aca hacemos la logica de preguntar que valor tiene el input oculto , si es cero , entonces es POST y es el id del usuario entonces es un PUT -->
        <input type="hidden" id="id_categoria" value="<%=data[0] ? data[0].id : '0' %>">
    </div>

</form>